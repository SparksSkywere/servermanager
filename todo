# TODO List (Status-tracked)

1. [WIP] Finalise the dashboard's primary functions (executable starts and stops, process tracking PID)
2. [WIP] Split all files into appropriate folders for hosts and subhosts (master and slave)
3. [WIP] Make sure the cross host-platform is working between the dashboards (web and local)
4. [TODO] Clusterise the hosts so that you can setup a master host that slave hosts, this will allow machines on the same network to communicate easier, only internal for now
5. [TODO] Basic information will need to parse between hosts such as CPU, RAM, Disk space, network load, etc for load balancing between the clustered hosts
6. [WIP] The servermanager must encrypt all connections internally and externally during operation (https too)
7. [WIP] Limit permissions to standard user only for server hosting, main host connections can use admin (with services)
8. [WIP] Logging for all scripts
9. [WIP] Webhosting dashboard needs to support sql (MSSQL, MSSQLEXPRESS, Mysql, MariaDB) with forms to create and register accounts (inlude 2FA for logins)
10. [WIP] Admin dashboard to create, remove, alter user accounts with SQL
11. [WIP] Dashboard.py will deal with the instances that are created by other users, to assist with starting and stopping games
12. [TODO] Outbound ports to be limited and is to be seperated from the main host and kept on a seperate machine to handle web requests, must support IIS, Apache2 and Nginx and simplicity implied to keep website loading to a minimal.
13. [WIP] User customisation features, admin access is to be kept for 2 users (root and host) and must authenticate using 2FA (enforced)
14. [WIP] Easy creation and deletion, folder/file access for the users
15. [WIP] Limit to only what the user is paying for, this is to prevent a user accessing more than is needed (ram mainly, maybe able to lock down core count?)
16. [WIP] Process consoles are open to the user allowing commands to be put through directly
17. [TODO] Payment tracker
18. [WIP] During installation of the servermanager 2 directories are to be made, 1 for steamcmd and the other for "other servers" like Minecraft
19. [WIP] Download of files, if multiple files it is to be zipped and downloaded
20. [WIP] Uploading files as to allow extraction for server hosting, the space given per folder generation allows one large zip to uploaded, extracted and then deployed or individual files can be uploaded.
21. [TODO] Dedicated debugging to test all the functions of the scripts and to see how it will react in certain situations such as a host failure
22. [TODO] A status website to show the status of api's or processes that are running to show the end users if something has gone down or wrong
23. [WIP] Emailing services to send out emails for service updates, tos, etc for a more professional approach
24. [TODO] Add a list function to the online dashboard to setup server states faster, could have a way of running multiple instances on one process? To limit downloading
25. [WIP] Binding to outbound and inbound ports (add firewall functions)
26. [WIP] Make sure some services are handled correctly like java version hunting, each of the versions are to be listed

# Legend:
# [TODO] - Not started
# [WIP]  - Work in progress / partially implemented
# [DONE] - Fully implemented (remove from list or move to completed section)